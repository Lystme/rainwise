# RainWise – example environment file

# Copy this file to `.env` and replace the placeholder values before running

# 1. OWM\_API\_KEY         – OpenWeatherMap API token (free tier is fine)

# 2. AIRFLOW\_SECRET\_KEY  – 32‑byte hex string; run `python -c "import os, binascii; print(binascii.hexlify(os.urandom(16)).decode())"`

# 3. AIRFLOW\_FERNET\_KEY  – Fernet key for Airflow metadata encryption; run `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`

OWM\_API\_KEY=replace\_me
AIRFLOW\_SECRET\_KEY=replace\_me
AIRFLOW\_FERNET\_KEY=replace\_me
